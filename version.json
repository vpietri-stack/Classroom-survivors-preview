{
  "version": "2026-09-10a",
  "deployedAt": "2026-09-10",
  "notes": "SR newer-wins + stuck-flag fix (2026-09-10a). Server saveAnalytics: each SR update carries monotonic srSeq (shouldApplySr: legacy/no-seq applies as today; newer applies; replay/stale ignored — no double increment); response confirms srApplied + srSeq. Client: finalizeSession stamps Date.now() srSeq (persisted csPendingSRSeq); flush sends SR triple only when newer than confirmed (obsolete suppressed locally, events still ship; increment rides only with its state); confirm-then-clear advances csConfirmedSrSeq watermark. Probe result baked in: write-path absence confirmed (0 sessions in live doc since Aug-21, archive holds none post-Aug-29, dashboard replay 0+5=5/10 correct) — no dashboard change. Tests: shouldApplySr x5 (auto-archive, 14), srSeq block a-f (flush-deadline, 73)."
}
