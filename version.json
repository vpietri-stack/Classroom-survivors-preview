{
  "version": "2026-09-08a",
  "deployedAt": "2026-09-08",
  "notes": "Doris Sep-7/8 incident set (2026-09-08a). (1) COOLDOWN FLOOR: a long-term successful student can have EVERY sentence pair on SR cooldown (harness-proven on Doris's live data: 148/148 cooldown, E1 returned null -> dead session). sortPoolBySR now keeps dropped cooldown entries on sorted._cooldown; pickWithNewQuota serves least-overdue first; getStudySentencePairsSubRoundSR reports group 4 (not 6-empty) for all-cooldown pages + tops up to 3 pairs. Practice beats a dead session. (2) STICKY PAGE-ADVANCE: the auto-advance decision (page 43->48) evaporated when updateStudent failed, then re-decided 'stay' at sessionCount 201 on 5 newly-due pairs. Pending page now persists in csPendingPageAdvance, re-applies at login before the normal check, clears only on server confirm. (3) CHECK HARDENING: normMatchText() (whitespace-collapse + lowercase) in study checkRoundF + game checkSentenceMatch — template-indented tile text can no longer fail a visually-correct placement forever. (4) DRAIN REPORT: queueDrain type:device event at every login describes the backlog (queueLen/nSession/nExercise/oldestQueuedTs/pendingSR/pendingIncr) so the next login after any silent completion-flush failure is self-describing. Tests: Rule5/Rule6 in test_round_e_dedup.js (16 tests), sticky-advance + drain-report blocks in test_session_flush_deadline.js (65 tests). Stamps -> 2026-09-08a; sr_engine/game/study per-file ?v= bumped."
}
